{
	"name": "study_comp",
	"version": "1.0.0",
	"description": "The plugin that's able to start playing a song as soon as the person started to sit and study. The playlist is selected beforehand from the UI.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "YoungStorm",
	"license": "ISC",
	"volumio_info": {
		"prettyName": "study_comp",
		"icon": "fa-lightbulb-o",
		"plugin_type": "system_controller"
	},
	"dependencies": {
		"fs-extra": "^0.28.0",
		"kew": "^0.7.0",
		"v-conf": "^1.4.0"
	}
}
